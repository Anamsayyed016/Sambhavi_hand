# Managed by Sambhavi deploy — AFTIONIX must remain on port 3000.
# Active Sambhavi backend port is switched between 3001 and 3002.
upstream sambhavi_backend {
    server 127.0.0.1:3001;
    keepalive 64;
}
