# Install to /etc/nginx/conf.d/sambhavi-upstream.conf (http context — not inside server {})
# Managed by Sambhavi deploy.sh — AFTIONIX uses port 3000 separately
upstream sambhavi_backend {
    server 127.0.0.1:3001;
}
